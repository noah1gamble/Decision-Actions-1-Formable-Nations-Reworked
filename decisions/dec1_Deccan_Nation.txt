country_decisions = {

	deccan_nation = {
		major = yes
		potential = {
			is_former_colonial_nation = no
			is_playing_custom_nation = no
			is_colonial_nation = no
			religion_group = muslim
			NOT = {
				has_country_flag = formed_deccan_nation_flag
				exists = MUG
				exists = BHA
				exists = HIN
				exists = DEC
				tag = MUG
				tag = BHA
				tag = HIN
				tag = DEC
		 	}
			OR = {
				culture_group = dravidian
				primary_culture = marathi
			}
			OR = {
				ai = no
				AND = {
					ai = yes
					num_of_cities = 6
				}
			}
		}
		provinces_to_highlight = {
			OR = {
				region = deccan_region
			}
			OR = {
				NOT = { owned_by = ROOT }
				NOT = { is_core = ROOT }
			}
		}
		allow = {
			NOT = { exists = DEC }
			is_at_war = no
			adm_tech = 5
			is_free_or_tributary_trigger = yes
			custom_trigger_tooltip = { # Deccan region
				tooltip = form_deccan_nation_tt_1
				deccan_region = {
					type = all
					owned_by = ROOT
				}
			}
		}
		effect = {
			change_tag = DEC # Country TAG
			set_country_flag = formed_deccan_nation_flag # Country Formation Flag
			custom_tooltip = form_deccan_tt_2 # New Missions
			custom_tooltip = dec1_decisions_blank	
			DEC = { # Capital change and bonuses				
				set_capital = 522 # Delhi
				522 = {
					add_base_tax = 1
					add_base_production = 1
					add_base_manpower = 1
				}
				# Government Rank Change
				custom_tooltip = dec1_decisions_blank				
				custom_tooltip = dec1_government_rank_2
				# Dharma DLC check to prevent ugly tooltip
				if = {
					limit = {
						has_dlc = "Dharma"
					}
					custom_tooltip = dec1_government_change_monarchy # Government Type Change
				}				
				# Reform Change
				add_government_reform = autocracy_reform
				custom_tooltip = dec1_decisions_blank	
				# Claims
				bengal_region = {
					limit = {
						NOT = { owned_by = ROOT }
					}
					add_permanent_claim = DEC
				}
				hindusthan_region = {
					limit = {
						NOT = { owned_by = ROOT }
					}
					add_permanent_claim = DEC
				}
				west_india_region = {
					limit = {
						NOT = { owned_by = ROOT }
					}
					add_permanent_claim = DEC
				}
				deccan_region = {
					limit = {
						NOT = { owned_by = ROOT }
					}
					add_permanent_claim = DEC
				}
				coromandel_region = {
					limit = {
						NOT = { owned_by = ROOT }
					}
					add_permanent_claim = DEC
				}			
				# Our Place in the Empire Custom Event
				if = {
					limit = {
						is_part_of_hre = yes
					}
					country_event = { id = dec1_events.1 }
				}	
				hidden_effect = { # Hidden Effects to fix bad tooltips	
					# Government Rank change				
					if = {
						limit = {
							NOT = {
								government_rank = 2
								}
							}
						set_government_rank = 2
					}
					# Government Type Change
					if = {
						limit = {
							NOT = {
								government = monarchy
								}
							}
						change_government = monarchy
					}																		
				}
				if = {
					limit = {	 # Custom Ideas check and ROOT check
						OR = {
							has_custom_ideas = no
							NOT = { 
								has_idea_group = ALP_ideas 
							}
						}
					}
					country_event = { 
						id = ideagroups.1
					}
				}
				country_event = {
					id = dec1_events.3	# Custom Missions change
				}
				custom_tooltip = dec1_decisions_blank
				add_country_modifier = {
					name = dec1_tough_terrain_castles	# National Ability
					duration = 49275
				}			
				add_country_modifier = {
					name = dec1_increased_centralization	# Increased Centralization
					duration = -1
				}
			}					
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
}
